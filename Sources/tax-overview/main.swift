// tax-overview CLI — Phase 4 stub (T002). Real logic wired in its user-story phase.
import Foundation
import FinanceWorkspaceKit

FileHandle.standardError.write(Data("usage: tax-overview --workspace <path> [flags]\n".utf8))
exit(2)
